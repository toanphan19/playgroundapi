# Playground API

A simple sandbox for various personal apps.

# Tech

This project FastAPI for the API server, `uv` for dependency management.

```bash
# Install dependencies
uv sync

# Run the dev server
uv run fastapi dev app/main.py

# Run all tests
uv run pytest
```
